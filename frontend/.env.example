# Production: point at your deployed API (Fly.io, Koyeb, etc.)
# Use the same host for Socket.IO — no trailing slash.
VITE_API_URL=https://mlsa-feedback-api.fly.dev/api
VITE_SOCKET_URL=https://mlsa-feedback-api.fly.dev

# Local dev: leave unset to use Vite proxy (/api -> http://localhost:5000)
