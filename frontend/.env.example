# Production: point at your deployed API (Render, Railway, etc.)
# Use the same host for Socket.IO — no trailing slash.
VITE_API_URL=https://mlsa-feedback-api.onrender.com/api
VITE_SOCKET_URL=https://mlsa-feedback-api.onrender.com

# Local dev: leave unset to use Vite proxy (/api -> http://localhost:5000)
